{
  "name": "@atmtfy/elite",
  "version": "0.0.1",
  "description": "Elite Shopify 2.0 Theme. Based on Shopify's \"Dawn\", but with preprocessing, etc",
  "main": "index.js",
  "author": "Automatify",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@atmtfy/schema-loader": "^0.0.5",

    "normalize.css": "^8.0.1",
    "swiper": "^6.8.0"
  },
  "scripts": {
    "start": "pnpm  build && run-p watch serve",
    "dev": "run-p watch serve",
    "deploy": "pnpm webpack -env=production && cd dist && shopify theme push",
    "theme-check": "pnpm  webpack --env=development && cd dist && shopify theme check",
    "pull": "cd dist && rm .shopifyignore && touch .shopifyignore && shopify theme pull",
    "push": "pnpm  webpack --env=production && cd dist && shopify theme push --unpublished --nodelete",
    "push:new": "pnpm  webpack --env=production && cd dist && rm .shopifyignore && touch .shopifyignore && shopify theme push --unpublished",
    "push:dry": "cd dist && shopify theme push --unpublished --nodelete",
    "update": "pnpm  webpack -- --env=production && cd dist && shopify theme push --nodelete -x 'config/settings_data.json' -x 'templates/*.json'",
    "publish": "pnpm  webpack -- --env=production && cd dist && shopify theme push --publish --nodelete ",
    "watch": "pnpm  webpack --watch --env=development",
    "serve": "cd dist && shopify theme serve",
    "build": "pnpm  webpack -- --env=production"
  },
  "devDependencies": {
     "file-loader": "^6.2.0",
    "merge-jsons-webpack-plugin": "^2.0.0-alpha",
    "clean-webpack-plugin": "^4.0.0-alpha.0",
    "concurrently": "^6.2.0",
    "copy-webpack-plugin": "^9.0.1",
    "css-loader": "^6.2.0",
    "glob": "^7.1.7",
    "merge-json-webpack-plugin": "^5.1.0",
    "mini-css-extract-plugin": "^2.1.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.36.0",
    "sass-loader": "^12.1.0",
    "webpack": "^5.51.1",
    "webpack-cli": "^4.7.2",
    "webpack-concat-files-plugin": "^0.5.2",
    "webpack-remove-empty-scripts": "^0.7.1"
  }
}
